const Scene = ({ children }: { children: React.ReactNode }) => (
  <p className="font-bold uppercase text-center tracking-widest text-stone-900 dark:text-stone-100 text-xs mb-4">
    {children}
  </p>
);

export default Scene;
